#!/usr/bin/env bash
# Web servers are the piece of software
# generating and serving HTML pages
# let’s install one

# Autor: Said LAMGHARI

sudo apt-get update
sudo apt-get -y install nginx

sudo 'echo "Hello World!" | tee /var/www/html/index.html'

sudo fw allow 'Nginx HTTP'

sudo sed -i 's/^\(\s*listen \[::\]:80\s*\)#/\1/' /etc/nginx/sites-available/default

sudo service nginx restartV
